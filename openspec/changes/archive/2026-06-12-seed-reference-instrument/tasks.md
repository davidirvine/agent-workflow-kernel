## 1. Repoint the manifest

- [x] 1.1 In `kernel-manifest.json`, change the three `instrumentStubs` sources for `presets/svelte-faust-synth` so each equals its target: `"faust/synth.dsp": "faust/synth.dsp"`, `"src/param-schema.js": "src/param-schema.js"`, `"src/components/InstrumentPanels.svelte": "src/components/InstrumentPanels.svelte"`. Run `npx prettier --write kernel-manifest.json`, then `scripts/check-manifest.sh` (must pass: sources exist, no target in `paths`). Commit `chore(manifest): repoint instrumentStubs sources to the reference instrument` — the per-commit type is `chore` because this is a manifest data swap with no release bump of its own; the **PR title** carries the release type `feat(generator): seed generated apps with the reference instrument` (the PR title is the release-parse input per `CLAUDE.md`, so the user-visible "feature" lands there).

## 2. Remove the redundant blank stubs

- [x] 2.1 Delete `presets/svelte-faust-synth/stubs/synth.dsp`, `stubs/param-schema.js`, `stubs/InstrumentPanels.svelte`, and the drift-guard `stubs/stubs.test.js`; remove the now-empty `presets/svelte-faust-synth/stubs/` directory. Confirm nothing under `presets/svelte-faust-synth/src/` imports from `stubs/` (the unrelated `src/lib/stubs/{fs,url}.js` shims are untouched). Commit `chore(preset): drop the redundant blank instrument stubs`.
- [x] 2.2 In `presets/svelte-faust-synth/vite.config.js`, remove the dead `'stubs/**/*.test.js'` entry from the Vitest `test.include` glob and its explanatory comment (leave `'src/**/*.test.js'`). Run `npx prettier --write presets/svelte-faust-synth/vite.config.js` (it is a JS file — `shfmt` is shell-only and does not apply) and `cd presets/svelte-faust-synth && npm run build` (build + vitest must pass with no missing-test error). Commit `chore(preset): drop the dead stubs test glob from vite config`.

## 3. Gate the new behavior

- [x] 3.1 In `scripts/generate-assert.sh`, add a positive assertion (after the existing `instrumentStubs` target-existence loop) that the emitted instrument is the reference, not a blank. Prefer a **structural** check over hard-coded param names so the gate survives a future reference-instrument swap: assert the emitted `src/param-schema.js` has a non-empty `PARAM_SCHEMA` with at least one `knob` descriptor (`PARAM_NAMES`/`KNOB_PARAMS` non-empty), and that `faust/synth.dsp` is non-trivial (not the old silent-`process = silence` stub). The current reference's `attack`/`release` may be referenced as an illustrative example but MUST NOT be the load-bearing assertion. **Implementation hint:** prefer grep-based checks over evaluating the ESM schema from bash — e.g. match a `kind: 'knob'` (or `kind:'knob'`) entry in the emitted `src/param-schema.js` and a non-`silence` `process =` line in the emitted `faust/synth.dsp`; this avoids `node --input-type=module` portability caveats and matches how the rest of `generate-assert.sh` greps emitted files. Run `shfmt -w` then `shellcheck` on the script, then `scripts/generate-assert.sh` (must pass end-to-end). Commit `test(generator): assert generated apps seed the reference instrument`.

## 4. Update the spec prose the delta cannot reach

- [x] 4.1 In `openspec/specs/app-generation/spec.md`, update the Purpose paragraph: change "the instrument blanked from committed stubs" to "the instrument seeded from the preset's committed reference instrument". (The Requirement-level changes are carried by this change's delta specs and land via `/opsx:sync`; the Purpose prose is not delta-managed, so edit it directly.) Run `npx prettier --write` on the file. Commit `docs(spec): describe seeded reference instrument in app-generation purpose`.

## 5. Update the docs that describe the old behavior

- [x] 5.1 In `STACK.md`, update every passage that says a generated app starts silent/blank (e.g. "The instrument starts as a silent stub DSP until you fill in `faust/synth.dsp`…" in "Using new-app.sh", and any matching line in "Using sync-kernel.sh" / setup notes) to state that a fresh app starts seeded with the preset's reference A/R sine instrument, which the developer then edits. Run `npx prettier --write STACK.md`. Commit `docs(stack): note generated apps seed the reference instrument`.
- [x] 5.2 Update the header comments in the reference instrument files — `presets/svelte-faust-synth/faust/synth.dsp`, `src/param-schema.js`, `src/components/InstrumentPanels.svelte` — that currently say `new-app.sh` "blanks" them, to state they are seeded verbatim into a fresh app as its starting instrument (and remain app-owned/never re-synced thereafter). Note: `new-app.sh`'s `--help`/usage text does **not** reference stubs (verified), so no change is needed there — but check the script's in-line banner/echo lines for any stale "silent stub" wording and update those. Run the preset build to confirm the DSP/schema still compile. Commit `docs(preset): correct stale "blanked at generation" comments`.

## 6. End-to-end verification

- [x] 6.1 Run the full completion-gate suite and confirm green: `scripts/check-manifest.sh`, `scripts/check-identity-leak.sh`, `scripts/generate-assert.sh`, `scripts/smoke-app.sh`, and `cd presets/svelte-faust-synth && npm ci && npm run build` (preset build + vitest, including the power-off-silence and chassis-purity tests). Spot-check a generated app: `scripts/new-app.sh --name seed-check --output /tmp/seed-check` then confirm `/tmp/seed-check/src/param-schema.js` carries the reference schema and `/tmp/seed-check/faust/synth.dsp` is the A/R sine. **Also explicitly verify the incoherence fix this change delivers**: run the generated app's own Vitest (`cd /tmp/seed-check && npm install && npm test` — or at minimum the traveling `src/state/synth.test.js`) and confirm it now passes against the seeded reference schema, where the old blank seed would have failed its `attack`/`release` assertions. No commit (verification only); record results for the completion gate.
